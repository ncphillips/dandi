<div class="page-block">
  <%= turbo_frame_tag @campaign do %>
    <div class="card stack vertical gap-4 p-4">
      <h1><%= @campaign.title %></h1>
      <p><%= @campaign.description %></p>
    </div>
  <% end %>
</div>
